# Phira 玩家接入指南

本文档面向想要连接本服务器、浏览并游玩谱面的玩家。

> 服务器管理员：公开仓库不会附带实际服务器地址。发布本指南前，请填写下方的 IP、证书要求和多人地址。若不希望把这些信息提交到 Git，请复制本文件到 `data/site-docs/USER.md`，在 `.env` 中设置 `USER_GUIDE_PATH=data/site-docs/USER.md`；`data/` 目录默认不会被 Git 提交。

## 向管理员确认连接信息

开始前，请向服务器管理员取得以下内容：

| 项目 | 管理员提供的值 |
| --- | --- |
| 服务器 IP | `<例如：203.0.113.10；仅局域网使用时可填写局域网 IP>` |
| 是否需要开启“不安全模式” | `<是 / 否>` |
| PMP+ 多人游戏地址（如果启用） | `<主机名或 IP:端口>` |

如果管理员没有提供其中某项，请先询问管理员，不要照抄示例地址。

## 安装 DNS 改写工具

在手机的应用商店中搜索 `DNS Override`，或安装其他支持“将指定域名指向指定 IP”的工具。首次使用时，系统可能会要求允许它创建本地 VPN；这类工具通常通过本地 VPN 完成 DNS 改写。

你只需要改写下面指定的 Phira API 域名，不要把所有网络流量都改到该服务器。

## 添加改写规则

在 DNS 改写工具中新建一条规则：

| 字段 | 填写内容 |
| --- | --- |
| 域名 / 主机 / API 地址 | `phira.5wyxi.com` |
| 指向 / 覆盖为 | 管理员提供的公网 IP |

填写时注意：

- 域名中不要包含 `https://`、端口、斜杠或路径。
- 保存后，确认这条规则已经启用。
- DNS 规则只改变域名对应的 IP，不能设置或转换端口。原版 Phira 会使用 HTTPS `443`；如果工具额外显示端口，请使用 `443`。
- 只添加 `phira.5wyxi.com` 这一条规则，不要把所有域名或所有网络流量都指向该服务器。

> 这条规则会把原版 Phira 发往官方 API 域名的请求交给私人服务器处理。私人服务器会处理私有谱面请求，并可能代理登录等其他 API 请求。仅连接你信任的管理员提供的服务器。

## 在 Phira 中浏览和游玩谱面

1. 确认 DNS 改写规则已经启用。
2. 从后台彻底关闭 Phira，再重新打开。
3. 正常登录 Phira。
4. 打开谱面浏览页面，选择本服务器提供的谱面。
5. 进入谱面详情页，下载谱面后即可游玩。

只浏览和游玩谱面通常不需要注册管理后台账号。上传、审核或管理谱面时，才需要管理员为你开通相应权限。

## 无法连接时

按以下顺序检查：

1. 确认 DNS 改写工具正在运行，规则中的域名和 IP 与管理员提供的一致。
2. 确认域名准确填写为 `phira.5wyxi.com`，没有带 `https://`、端口或路径。
3. 将 Phira 从后台划掉，再重新打开。
4. 关闭后重新启用 DNS 改写规则，以刷新 DNS。
5. 确认服务器入口使用外部 HTTPS `443`；玩家不能通过 DNS 规则把连接改到 `8443` 等其他端口。
6. 如果出现证书错误，并且管理员明确要求开启不安全模式，请在 Phira 的“设置 → 调试”中开启“不安全模式”，然后重启 Phira。
7. 仍然失败时，把错误提示、使用的网络类型以及当前时间发给服务器管理员。

> “不安全模式”会降低连接安全性。仅在你信任该服务器且管理员明确要求时开启；返回官方服务器后建议关闭。

## 多人游戏（可选）

只有服务器启用了 PMP+ 时，本服务器才能提供多人游戏。请使用管理员给出的 PMP+ 主机名或 IP 与游戏端口；如果管理员没有提供，表示该功能可能未启用。

PMP+ 的多人游戏地址与上面的 HTTPS API 地址不一定相同，不要自行猜测端口。

## 返回官方服务器

关闭或删除刚才添加的 DNS 改写规则，然后彻底关闭并重新打开 Phira。关闭规则后，Phira 将不再连接这台私人谱面服务器。
