# USER.md · Phira 谱面游玩接入指南

本文档写给**想用 Phira 游玩本服务器谱面的玩家**。

你不需要注册管理面板、不需要上传谱面，只要让 Phira 客户端连到本服务器的 API 地址，就能浏览和游玩这里的谱面。

## 准备工作

- 已经安装 Phira 客户端
- 一台手机或电脑，以及一个支持“把某个域名指向指定 IP”的 DNS 改写（DNS Override）工具

## 只需要两步

### 1. 下载并安装 DNS Override 软件

在应用商店搜索 `DNS Override` 或任意支持“自定义域名 → IP”的工具，安装后允许它创建本地 VPN（iOS 上会要求开启 VPN 权限，这是正常的，它只在本地改写 DNS）。

### 2. 添加一条改写规则

在 DNS Override 中新建一条规则，填写以下内容：

| 字段 | 填写内容 |
| --- | --- |
| 域名 / 主机 / API 地址 | `phira.5wyxi.com` |
| 指向 / 覆写为 | `203.0.113.10` |
| 协议 / 端口 | HTTPS / 443（一般不需要手动填） |

注意：

- 不要填写 `https://` 前缀，也不要带 `/` 或路径，只填 `phira.5wyxi.com`
- 保存后**开启/启动**这条 Override 规则

## 打开 Phira

1. 确认 DNS Override 已处于开启状态
2. 彻底关闭 Phira 后重新打开（不要只切后台，直接把应用划掉）
3. 正常登录、浏览谱面列表，找到想要的谱面并开始游玩

如果 Phira 提示无法连接或证书错误：

1. 打开 Phira **设置 → 调试**
2. 打开「**不安全模式**」
3. 重新启动 Phira

> 「不安全模式」只用于连接本类本地谱面服务；连接官方服务器时建议关闭。

## 常见问题

**只玩谱面需要注册账号吗？**
不需要。只有上传和管理谱面的人需要在管理面板注册。

**会影响我访问其他网站吗？**
不会。这条规则只把 `phira.5wyxi.com` 指向指定 IP，其他域名和 App 不受影响。

**改了之后打不开 Phira 或者加载空白？**

- 确认 Override 已开启
- 确认填写的域名没有 `https://`、没有端口或路径
- 把 Phira 从后台划掉后重新打开
- 可以先把 Override 关掉再打开一次，刷新 DNS

**怎么回到官方服务器？**
把 Override 关闭或删除刚才的规则，再重启 Phira 即可。

**服务地址以后会变吗？**
如果服务器地址有变化，只需要把 `203.0.113.10` 改成新的地址，无需重装 Phira。
